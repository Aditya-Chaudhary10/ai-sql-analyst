# AI SQL Analyst

Natural language → safe SQL → PostgreSQL.
Tech stack: Next.js (frontend), FastAPI (backend), LangGraph (AI), Redis (queue), PostgreSQL (DB).

Folder structure:
- frontend/
- backend/
- langgraph/
- db/
- notebooks/
